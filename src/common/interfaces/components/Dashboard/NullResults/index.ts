/* This code snippet is defining an interface named `NullResultsFoundProps` in a TypeScript file
`NoResultsFound.interface.ts`. The interface has two optional properties: `text`, which can be a
string or a number, and `module`, which is a string. This interface can be used to define the shape
of objects that have these properties in TypeScript code. */
// NoResultsFound.interface.ts
export interface NullResultsFoundProps {
    text?: string | number;
    module?: string
}