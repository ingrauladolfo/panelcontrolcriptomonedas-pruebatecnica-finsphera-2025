/* This code snippet is importing the `useLanguage` and `LanguageProvider` functions from a file
located at "./Language/LanguageContext" and then exporting them. This allows other files to import
these functions using the `import` statement and use them in their own code. */
import { useLanguage, LanguageProvider } from "./Language/LanguageContext";

export { useLanguage, LanguageProvider }